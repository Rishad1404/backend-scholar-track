model ApplicationScreening {
  id            String @id @default(uuid())
  applicationId String @unique 
  reviewerId    String 

  passed  Boolean
  comment String? @db.Text

  reviewedAt DateTime @default(now())
  updatedAt  DateTime @updatedAt

  isDeleted Boolean   @default(false)
  deletedAt DateTime?

  // Relations
  application Application @relation(fields: [applicationId], references: [id], onDelete: Cascade)
  departmentHead DepartmentHead @relation(fields: [reviewerId], references: [id], onDelete: Restrict)


  @@index([reviewerId], name: "idx_screening_reviewerId")
  @@index([isDeleted], name: "idx_screening_isDeleted")
  @@map("application_screenings")
}
